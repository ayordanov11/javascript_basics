let petName = 'Hal';
//can't define a new value on the same variable name => error;
//let petName = 'Spot';
petName = 'Spot';

console.log(petName);

//1. You can't define a variable more than once!
//2. There are rules related to variable names;
//3. Variables can only start with letter, _, $ - not anything else
//4. Variables names cannot be reserved keywords - let, 


//let 3 = 2;
//let result = 3 + 4;